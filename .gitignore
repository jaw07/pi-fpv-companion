__pycache__/
*.pyc
*.pyo
.venv/
venv/
.env
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
build/
dist/

# Model weights — large binaries, do not commit
*.rpk
*.ncnn
# EXCEPT the deployed default model — ships with the code so deploys are self-contained.
!models/imx500_network_yolo11n_visdrone416_pp.rpk
# Other export-pipeline build artifacts stay ignored.
models/*.zip
models/*best*.pt
models/*_labels.txt
*.param
*.bin
*.onnx
*.pt

# Editor / OS
.DS_Store
.vscode/
.idea/

# Local config overrides
config/local.yaml
config/*.local.yaml
